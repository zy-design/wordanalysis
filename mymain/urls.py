from mymain import views
from django.conf.urls import url, include

app_name = 'mymain'
urlpatterns = [
    # url('index', views.index),
    # url('search', views.search),
    # url('download_file', views.download_file),
    # url('download', views.download),
]
